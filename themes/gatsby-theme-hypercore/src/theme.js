import { createTheme } from "@material-ui/core/styles"

let theme = createTheme({
  palette: {
    primary: {
      main: "#333",
    },
    secondary: {
      main: "#999",
    },
  },
})

export default theme
